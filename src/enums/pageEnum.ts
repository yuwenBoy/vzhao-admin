export enum PageEnum {
    // basic login path
    BASE_LOGIN = '/login',
    // basice home path
    BASE_HOME = '/dashboard',
    // bsice test page path
    BASE_DEMO = '/demo',
    // error page path
    ERROR_PAGE = '/exception',
}