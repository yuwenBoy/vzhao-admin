export enum PageEnum {
    // basic login path
    BASE_LOGIN = '/login',
    // basice home path
    BASE_HOME = '/home',
    // bsice test page path
    BASE_DEMO = '/demo',
    // error page path
    ERROR_PAGE = '/exception',
}